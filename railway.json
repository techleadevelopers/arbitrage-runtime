{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cargo build --release && mkdir -p bin && cp target/release/mev-sweeper bin/mev-sweeper"
  },
  "deploy": {
    "startCommand": "./bin/mev-sweeper",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
